<% if @sub_categories.exists? %>
  Sub-Sub Category: <%= select_tag 'sub_sub_category_id', options_for_select([["Select",""]]+SubSubCategory.where("sub_category_id = ?", params[:sub_category_id]).map{|a| [a.name, a.id]}) %> 
<% end %>